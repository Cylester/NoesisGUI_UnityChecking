<UserControl x:Class="NoesisGUICSharp_Testing.Views.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:views="clr-namespace:NoesisGUICSharp_Testing.Views"
             xmlns:modelviews="clr-namespace:NoesisGUICSharp_Testing.ModelViews"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <modelviews:HomeModelView/>
    </UserControl.DataContext>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="10*"/>
            <ColumnDefinition Width="30*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="20*"/>
            <RowDefinition Height="50*"/>
            <RowDefinition Height="20*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="20" MinWidth="120">
            <TextBlock Text="Main menu" Foreground="White" TextAlignment="Center" FontSize="26" Margin="0,0,0,10"/>
            <Button Content="Play" Margin="0,0,0,10" Command="{Binding ButtonStartGameCommand}"/>
            <Button Content="Settings" Margin="0,0,0,10" Command="{Binding ButtonSettingsCommand}"/>
            <Button Content="About" Margin="0,0,0,10" Command="{Binding ButtonAboutCommand}"/>
            <Button Content="Exit"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Grid.Column="1" VerticalAlignment="Center">
            <ContentControl Content="{Binding CurrentView}"/>
        </StackPanel>
    </Grid>
</UserControl>
